<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>RTSP Live Stream Viewer | StreamVision</title>
    <meta
      name="description"
      content="StreamVision - View your RTSP camera feeds live with a powerful, modern, and secure web viewer."
    />
    <meta
      name="keywords"
      content="live cctv camera feed, watch security camera online, ip camera live stream, remote video surveillance viewer, public webcams, real-time monitoring, rtsp hls stream player"
    />
    <meta name="author" content="Amrit, Developer of StreamVision" />

    <link rel="canonical" href="https://cctvcameralive.in/" />
    <meta name="robots" content="index, follow" />
    <meta property="og:title" content="StreamVision | RTSP Live CCTV Viewer" />
    <meta
      property="og:description"
      content="Real-time RTSP CCTV camera streaming using HLS and analytics."
    />
    <meta property="og:image" content="https://cctvcameralive.in/images/banner.svg" />
    <meta property="og:url" content="https://cctvcameralive.in" />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="StreamVision | RTSP Live CCTV Viewer" />
    <meta
      name="twitter:description"
      content="Real-time RTSP CCTV camera streaming using HLS and analytics."
    />
    <meta name="twitter:image" content="https://cctvcameralive.in/images/banner.svg" />

    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
    <link rel="manifest" href="/site.webmanifest" />

    <link rel="preconnect" href="https://fonts.googleapis.com" crossorigin />
    <link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin />

    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="/css/style.css?v=5.3" nonce="<%= nonce %>" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      nonce="<%= nonce %>"
    />
    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"
      defer
      nonce="<%= nonce %>"
    ></script>
    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js"
      defer
      nonce="<%= nonce %>"
    ></script>

    <script src="https://cdn.jsdelivr.net/npm/hls.js@latest" defer nonce="<%= nonce %>"></script>

    <meta http-equiv="Cache-Control" content="no-store, no-cache, must-revalidate, max-age=0" />
    <meta http-equiv="Pragma" content="no-cache" />
    <meta http-equiv="Expires" content="0" />

    <style nonce="<%= nonce %>">
      @import url("https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap");

      * {
        font-family: "Inter", sans-serif;
      }

      .gradient-text {
        background: linear-gradient(to right, #63b3ed, #3182ce);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
      }

      .btn-gradient {
        background: linear-gradient(to right, #63b3ed, #3182ce);
        transition: all 0.3s ease;
      }

      .btn-gradient:hover {
        background: linear-gradient(to right, #3182ce, #2c5282);
        transform: translateY(-2px);
        box-shadow: 0 10px 25px -5px rgba(49, 130, 206, 0.3);
      }

      .btn-gradient:active {
        transform: translateY(0);
      }

      .card-hover {
        transition: all 0.3s ease;
      }

      .card-hover:hover {
        box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
        transform: translateY(-2px);
      }

      .loader {
        border-top-color: #3182ce;
        -webkit-animation: spinner 1.5s linear infinite;
        animation: spinner 1.5s linear infinite;
      }

      @-webkit-keyframes spinner {
        0% {
          -webkit-transform: rotate(0deg);
        }
        100% {
          -webkit-transform: rotate(360deg);
        }
      }

      @keyframes spinner {
        0% {
          transform: rotate(0deg);
        }
        100% {
          transform: rotate(360deg);
        }
      }

      .tech-icon {
        transition: all 0.3s ease;
      }

      .tech-icon:hover {
        transform: scale(1.1) rotate(5deg);
      }

      .shadow-glow {
        box-shadow: 0 0 20px rgba(99, 179, 237, 0.2);
      }

      .shadow-glow:hover {
        box-shadow: 0 0 30px rgba(99, 179, 237, 0.3);
      }

      .border-glow {
        border-color: rgba(99, 179, 237, 0.3);
      }

      .border-glow:hover {
        border-color: rgba(99, 179, 237, 0.6);
      }

      .bg-grid-pattern {
        background-image:
          linear-gradient(rgba(99, 179, 237, 0.1) 1px, transparent 1px),
          linear-gradient(90deg, rgba(99, 179, 237, 0.1) 1px, transparent 1px);
        background-size: 50px 50px;
      }

      .hero-gradient {
        background: linear-gradient(
          135deg,
          rgba(99, 179, 237, 0.1) 0%,
          rgba(49, 130, 206, 0.05) 100%
        );
      }

      .pulse-animation {
        animation: pulse 2s infinite;
      }

      @keyframes pulse {
        0% {
          box-shadow: 0 0 0 0 rgba(99, 179, 237, 0.7);
        }
        70% {
          box-shadow: 0 0 0 10px rgba(99, 179, 237, 0);
        }
        100% {
          box-shadow: 0 0 0 0 rgba(99, 179, 237, 0);
        }
      }

      .slide-down {
        animation: slideDown 0.5s ease forwards;
      }

      @keyframes slideDown {
        from {
          opacity: 0;
          transform: translateY(-20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
  </head>

  <body class="bg-gray-50 dark:bg-gray-900 text-gray-800 dark:text-gray-100 min-h-screen">
    <!-- Header -->
    <header
      class="sticky top-0 z-40 w-full bg-white/80 dark:bg-gray-900/80 backdrop-blur-md border-b border-blue-100 dark:border-gray-800"
    >
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <a href="/" class="flex items-center space-x-3">
            <div
              class="w-10 h-10 rounded-xl bg-gradient-to-br from-[#63b3ed] to-[#3182ce] flex items-center justify-center"
            >
              <i class="fas fa-video text-white"></i>
            </div>
            <div>
              <span class="text-xl font-bold gradient-text">StreamVision</span>
              <p class="text-xs text-gray-500 dark:text-gray-400">by Netwon Team</p>
            </div>
          </a>

          <div class="flex items-center space-x-4">
            <!-- Theme Toggle -->
            <button
              id="theme-toggle"
              class="p-2 rounded-lg bg-gray-100 dark:bg-gray-800 text-gray-600 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 transition-colors"
            >
              <i class="fas fa-moon dark:hidden"></i>
              <i class="fas fa-sun hidden dark:inline"></i>
            </button>

            <!-- Support Link -->
            <a
              href="https://service.cctvcameralive.in/"
              class="hidden sm:inline-flex items-center px-4 py-2 text-sm font-medium text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 transition-colors"
            >
              <i class="fas fa-life-ring mr-2"></i>
              Support
            </a>

            <!-- Admin Login Button -->
            <a
              href="/login"
              class="btn-gradient text-white font-medium rounded-xl px-5 py-2.5 text-sm inline-flex items-center shadow-sm"
            >
              <i class="fas fa-sign-in-alt mr-2"></i>
              Admin Login
            </a>
          </div>
        </div>
      </div>
    </header>

    <main class="overflow-x-hidden">
      <!-- Hero Section -->
      <section class="relative py-16 sm:py-20 lg:py-24 hero-gradient">
        <div class="absolute inset-0 bg-grid-pattern"></div>
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
          <div class="text-center mb-12">
            <!-- Update Banner -->
            <div
              class="inline-flex items-center mb-8 px-4 py-2 bg-blue-50 dark:bg-blue-900/30 rounded-full border border-blue-100 dark:border-blue-800"
            >
              <span
                class="text-xs bg-gradient-to-r from-[#63b3ed] to-[#3182ce] text-white px-3 py-1 rounded-full mr-2"
              >
                <i class="fas fa-rocket mr-1"></i>
                New
              </span>
              <a href="/updates" class="text-sm font-medium text-gray-700 dark:text-gray-300">
                StreamVision v2.0 is live! Enhanced streaming experience 🚀
              </a>
            </div>

            <!-- Main Heading -->
            <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold mb-6">
              <span class="block">Live</span>
              <span class="gradient-text">RTSP Stream Viewer</span>
            </h1>

            <!-- Subtitle -->
            <p class="text-lg sm:text-xl text-gray-600 dark:text-gray-400 max-w-3xl mx-auto mb-10">
              Securely view your RTSP camera feeds in real-time with our powerful web-based
              streaming platform
            </p>
          </div>

          <!-- Streaming Interface -->
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
            <!-- Input Card -->
            <div
              class="bg-white dark:bg-gray-800 rounded-2xl shadow-lg p-6 lg:p-8 border border-blue-100 dark:border-gray-700 card-hover"
            >
              <div class="flex items-center mb-6">
                <div
                  class="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-100 to-blue-50 dark:from-blue-900/30 dark:to-blue-800/20 flex items-center justify-center mr-4"
                >
                  <i class="fas fa-link text-blue-600 dark:text-blue-400 text-xl"></i>
                </div>
                <div>
                  <h3 class="text-xl font-bold text-gray-900 dark:text-white">Stream Input</h3>
                  <p class="text-sm text-gray-600 dark:text-gray-400">
                    Enter your RTSP URL to begin
                  </p>
                </div>
              </div>

              <div class="space-y-4">
                <div>
                  <label
                    for="rtspInput"
                    class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2"
                  >
                    <i class="fas fa-camera mr-2"></i>
                    RTSP Stream URL
                  </label>
                  <input
                    type="text"
                    id="rtspInput"
                    class="w-full px-4 py-3 bg-gray-50 dark:bg-gray-700 border border-blue-200 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-gray-900 dark:text-white transition-all"
                    placeholder="rtsp://username:password@ip_address:port/stream"
                  />
                </div>

                <div class="flex flex-col sm:flex-row gap-4 pt-4">
                  <button
                    id="playBtn"
                    class="btn-gradient text-white font-medium rounded-xl px-6 py-3.5 flex-1 inline-flex items-center justify-center"
                  >
                    <i class="fas fa-play mr-2"></i>
                    Start Stream
                  </button>

                  <button
                    id="stopBtn"
                    class="hidden bg-white dark:bg-gray-700 text-gray-700 dark:text-gray-300 border border-gray-300 dark:border-gray-600 font-medium rounded-xl px-6 py-3.5 flex-1 inline-flex items-center justify-center hover:bg-gray-50 dark:hover:bg-gray-600 transition-colors"
                  >
                    <i class="fas fa-stop mr-2"></i>
                    Stop Stream
                  </button>
                </div>

                <div class="text-xs text-gray-500 dark:text-gray-400 pt-2">
                  <i class="fas fa-info-circle mr-1"></i>
                  Ensure your camera supports RTSP and is accessible from this network
                </div>
              </div>
            </div>

            <!-- Video Preview Card -->
            <div
              class="bg-white dark:bg-gray-800 rounded-2xl shadow-lg overflow-hidden border border-blue-100 dark:border-gray-700"
            >
              <div class="p-6 border-b border-blue-50 dark:border-gray-700">
                <div class="flex items-center justify-between">
                  <div class="flex items-center">
                    <div
                      class="w-10 h-10 rounded-lg bg-gradient-to-br from-blue-100 to-blue-50 dark:from-blue-900/30 dark:to-blue-800/20 flex items-center justify-center mr-3"
                    >
                      <i class="fas fa-eye text-blue-600 dark:text-blue-400"></i>
                    </div>
                    <div>
                      <h3 class="font-bold text-gray-900 dark:text-white">Live Preview</h3>
                      <p class="text-sm text-gray-600 dark:text-gray-400">Stream output</p>
                    </div>
                  </div>
                  <div class="flex items-center space-x-2">
                    <div class="w-3 h-3 rounded-full bg-green-500 animate-pulse"></div>
                    <span class="text-sm text-green-600 dark:text-green-400">Live</span>
                  </div>
                </div>
              </div>

              <!-- Video Container -->
              <div id="videoContainer" class="hidden relative bg-gray-900 aspect-video">
                <!-- Loader -->
                <div
                  id="loader"
                  class="hidden absolute inset-0 flex items-center justify-center bg-gray-900/90"
                >
                  <div class="text-center">
                    <div
                      class="loader ease-linear rounded-full border-4 border-t-4 border-blue-500 h-12 w-12 mx-auto mb-4"
                    ></div>
                    <p class="text-blue-400 font-medium">Initializing stream...</p>
                    <p class="text-sm text-gray-400 mt-1">This may take a few moments</p>
                  </div>
                </div>

                <!-- Video Player -->
                <video
                  id="videoPlayer"
                  width="100%"
                  controls
                  class="w-full h-full hidden"
                  poster="https://images.unsplash.com/photo-1513475382585-d06e58bcb0e0?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80"
                ></video>
              </div>

              <!-- Placeholder State -->
              <div
                id="videoPlaceholder"
                class="bg-gradient-to-br from-gray-800 to-gray-900 aspect-video flex flex-col items-center justify-center p-6"
              >
                <div
                  class="w-20 h-20 rounded-full bg-gradient-to-br from-blue-900/50 to-blue-800/30 flex items-center justify-center mb-6 border-2 border-blue-700/30"
                >
                  <i class="fas fa-video text-blue-400 text-3xl"></i>
                </div>
                <h4 class="text-xl font-bold text-white mb-2">Stream Preview</h4>
                <p class="text-gray-400 text-center max-w-md">
                  Enter an RTSP URL and click "Start Stream" to view your live camera feed here
                </p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Technology Stack Section -->
      <section class="py-16 sm:py-20 lg:py-24 bg-white dark:bg-gray-900">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="text-center mb-12">
            <span
              class="inline-flex items-center px-4 py-2 rounded-full bg-blue-100 dark:bg-blue-900/30 text-blue-800 dark:text-blue-400 text-sm font-medium mb-4"
            >
              <i class="fas fa-microchip mr-2"></i>
              Technology Stack
            </span>
            <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold mb-6">
              Powered by
              <span class="gradient-text">Modern Technology</span>
            </h2>
            <p class="text-lg text-gray-600 dark:text-gray-400 max-w-3xl mx-auto">
              StreamVision leverages cutting-edge open-source technologies for exceptional
              performance and reliability
            </p>
          </div>

          <!-- Tech Cards -->
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-12">
            <!-- FFmpeg Card -->
            <div
              class="bg-gray-50 dark:bg-gray-800 rounded-2xl p-6 border border-blue-100 dark:border-gray-700 card-hover"
            >
              <div
                class="w-16 h-16 rounded-xl bg-gradient-to-br from-blue-100 to-blue-50 dark:from-blue-900/30 dark:to-blue-800/20 flex items-center justify-center mb-6 mx-auto tech-icon"
              >
                <i class="fas fa-video text-blue-600 dark:text-blue-400 text-2xl"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white text-center mb-2">
                FFmpeg
              </h3>
              <p class="text-gray-600 dark:text-gray-400 text-center text-sm">
                High-performance video processing and transcoding
              </p>
            </div>

            <!-- HLS Card -->
            <div
              class="bg-gray-50 dark:bg-gray-800 rounded-2xl p-6 border border-blue-100 dark:border-gray-700 card-hover"
            >
              <div
                class="w-16 h-16 rounded-xl bg-gradient-to-br from-blue-100 to-blue-50 dark:from-blue-900/30 dark:to-blue-800/20 flex items-center justify-center mb-6 mx-auto tech-icon"
              >
                <i class="fas fa-broadcast-tower text-blue-600 dark:text-blue-400 text-2xl"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white text-center mb-2">
                HLS Protocol
              </h3>
              <p class="text-gray-600 dark:text-gray-400 text-center text-sm">
                Adaptive streaming for smooth playback across devices
              </p>
            </div>

            <!-- Node.js Card -->
            <div
              class="bg-gray-50 dark:bg-gray-800 rounded-2xl p-6 border border-blue-100 dark:border-gray-700 card-hover"
            >
              <div
                class="w-16 h-16 rounded-xl bg-gradient-to-br from-blue-100 to-blue-50 dark:from-blue-900/30 dark:to-blue-800/20 flex items-center justify-center mb-6 mx-auto tech-icon"
              >
                <i class="fab fa-node-js text-blue-600 dark:text-blue-400 text-2xl"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white text-center mb-2">
                Node.js
              </h3>
              <p class="text-gray-600 dark:text-gray-400 text-center text-sm">
                Scalable backend runtime for real-time processing
              </p>
            </div>

            <!-- Express Card -->
            <div
              class="bg-gray-50 dark:bg-gray-800 rounded-2xl p-6 border border-blue-100 dark:border-gray-700 card-hover"
            >
              <div
                class="w-16 h-16 rounded-xl bg-gradient-to-br from-blue-100 to-blue-50 dark:from-blue-900/30 dark:to-blue-800/20 flex items-center justify-center mb-6 mx-auto tech-icon"
              >
                <i class="fas fa-bolt text-blue-600 dark:text-blue-400 text-2xl"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white text-center mb-2">
                Express.js
              </h3>
              <p class="text-gray-600 dark:text-gray-400 text-center text-sm">
                Fast and minimalist web application framework
              </p>
            </div>
          </div>

          <!-- Open Source Call-to-Action -->
          <div
            class="bg-gradient-to-r from-blue-50 to-blue-100 dark:from-blue-900/30 dark:to-blue-800/20 rounded-2xl p-8 border border-blue-200 dark:border-blue-800"
          >
            <div class="flex flex-col lg:flex-row items-center justify-between">
              <div class="mb-6 lg:mb-0 lg:mr-8">
                <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-3">
                  Open Source Community
                </h3>
                <p class="text-gray-700 dark:text-gray-300">
                  StreamVision is an open-source project. Join our community to contribute, suggest
                  features, or report issues.
                </p>
              </div>
              <a
                href="https://github.com/codebyamrit/StreamVision"
                target="_blank"
                class="btn-gradient text-white font-medium rounded-xl px-8 py-3.5 inline-flex items-center whitespace-nowrap"
              >
                <i class="fab fa-github mr-2"></i>
                Contribute on GitHub
              </a>
            </div>
          </div>
        </div>
      </section>

      <!-- Features Section -->
      <section class="py-16 sm:py-20 lg:py-24 bg-gray-50 dark:bg-gray-800/50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="text-center mb-12">
            <span
              class="inline-flex items-center px-4 py-2 rounded-full bg-blue-100 dark:bg-blue-900/30 text-blue-800 dark:text-blue-400 text-sm font-medium mb-4"
            >
              <i class="fas fa-star mr-2"></i>
              Key Features
            </span>
            <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold mb-6">
              Why Choose
              <span class="gradient-text">StreamVision?</span>
            </h2>
            <p class="text-lg text-gray-600 dark:text-gray-400 max-w-3xl mx-auto">
              A comprehensive streaming solution built for performance, security, and ease of use
            </p>
          </div>

          <!-- Features Grid -->
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <!-- Feature 1 -->
            <div
              class="bg-white dark:bg-gray-800 rounded-2xl p-6 border border-blue-100 dark:border-gray-700 card-hover"
            >
              <div
                class="w-12 h-12 rounded-lg bg-gradient-to-br from-blue-100 to-blue-50 dark:from-blue-900/30 dark:to-blue-800/20 flex items-center justify-center mb-6"
              >
                <i class="fas fa-bolt text-blue-600 dark:text-blue-400"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3">Low Latency</h3>
              <p class="text-gray-600 dark:text-gray-400">
                Real-time streaming with minimal delay, perfect for live surveillance and monitoring
                applications.
              </p>
            </div>

            <!-- Feature 2 -->
            <div
              class="bg-white dark:bg-gray-800 rounded-2xl p-6 border border-blue-100 dark:border-gray-700 card-hover"
            >
              <div
                class="w-12 h-12 rounded-lg bg-gradient-to-br from-blue-100 to-blue-50 dark:from-blue-900/30 dark:to-blue-800/20 flex items-center justify-center mb-6"
              >
                <i class="fas fa-shield-alt text-blue-600 dark:text-blue-400"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3">Secure Streaming</h3>
              <p class="text-gray-600 dark:text-gray-400">
                Encrypted streams with SSL/TLS and secure authentication to protect your video
                feeds.
              </p>
            </div>

            <!-- Feature 3 -->
            <div
              class="bg-white dark:bg-gray-800 rounded-2xl p-6 border border-blue-100 dark:border-gray-700 card-hover"
            >
              <div
                class="w-12 h-12 rounded-lg bg-gradient-to-br from-blue-100 to-blue-50 dark:from-blue-900/30 dark:to-blue-800/20 flex items-center justify-center mb-6"
              >
                <i class="fas fa-link text-blue-600 dark:text-blue-400"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3">Shareable Links</h3>
              <p class="text-gray-600 dark:text-gray-400">
                Generate time-limited public links to share streams without compromising security.
              </p>
            </div>

            <!-- Feature 4 -->
            <div
              class="bg-white dark:bg-gray-800 rounded-2xl p-6 border border-blue-100 dark:border-gray-700 card-hover"
            >
              <div
                class="w-12 h-12 rounded-lg bg-gradient-to-br from-blue-100 to-blue-50 dark:from-blue-900/30 dark:to-blue-800/20 flex items-center justify-center mb-6"
              >
                <i class="fas fa-mobile-alt text-blue-600 dark:text-blue-400"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3">Cross-Platform</h3>
              <p class="text-gray-600 dark:text-gray-400">
                Access streams from any device - desktop, tablet, or mobile with responsive design.
              </p>
            </div>

            <!-- Feature 5 -->
            <div
              class="bg-white dark:bg-gray-800 rounded-2xl p-6 border border-blue-100 dark:border-gray-700 card-hover"
            >
              <div
                class="w-12 h-12 rounded-lg bg-gradient-to-br from-blue-100 to-blue-50 dark:from-blue-900/30 dark:to-blue-800/20 flex items-center justify-center mb-6"
              >
                <i class="fas fa-sync text-blue-600 dark:text-blue-400"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3">Auto Reconnect</h3>
              <p class="text-gray-600 dark:text-gray-400">
                Automatic reconnection attempts if the stream drops, ensuring continuous monitoring.
              </p>
            </div>

            <!-- Feature 6 -->
            <div
              class="bg-white dark:bg-gray-800 rounded-2xl p-6 border border-blue-100 dark:border-gray-700 card-hover"
            >
              <div
                class="w-12 h-12 rounded-lg bg-gradient-to-br from-blue-100 to-blue-50 dark:from-blue-900/30 dark:to-blue-800/20 flex items-center justify-center mb-6"
              >
                <i class="fas fa-chart-line text-blue-600 dark:text-blue-400"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3">
                Performance Analytics
              </h3>
              <p class="text-gray-600 dark:text-gray-400">
                Monitor stream health, bandwidth usage, and performance metrics in real-time.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- About Developer Section -->
      <section class="py-16 sm:py-20 lg:py-24 bg-white dark:bg-gray-900">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="text-center mb-12">
            <span
              class="inline-flex items-center px-4 py-2 rounded-full bg-blue-100 dark:bg-blue-900/30 text-blue-800 dark:text-blue-400 text-sm font-medium mb-4"
            >
              <i class="fas fa-user-tie mr-2"></i>
              About the Developer
            </span>
            <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold mb-6">
              Meet the
              <span class="gradient-text">Creator</span>
            </h2>
          </div>

          <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
            <!-- Developer Info -->
            <div>
              <div
                class="bg-white dark:bg-gray-800 rounded-2xl p-8 border border-blue-100 dark:border-gray-700 shadow-lg"
              >
                <div class="flex items-center mb-6">
                  <div class="relative">
                    <img
                      src="https://avatars.githubusercontent.com/u/35948782?v=4"
                      alt="Amrit Sharma"
                      class="w-24 h-24 rounded-2xl border-4 border-white dark:border-gray-800 shadow-lg"
                    />
                    <div
                      class="absolute -bottom-2 -right-2 w-10 h-10 rounded-full bg-gradient-to-br from-blue-500 to-blue-600 flex items-center justify-center"
                    >
                      <i class="fas fa-code text-white text-sm"></i>
                    </div>
                  </div>
                  <div class="ml-6">
                    <h3 class="text-2xl font-bold text-gray-900 dark:text-white">Amrit Sharma</h3>
                    <p class="text-blue-600 dark:text-blue-400 font-medium">Full-Stack Developer</p>
                    <div class="flex items-center mt-2">
                      <div class="w-3 h-3 rounded-full bg-green-500 animate-pulse mr-2"></div>
                      <span class="text-sm text-gray-600 dark:text-gray-400">
                        Available for collaborations
                      </span>
                    </div>
                  </div>
                </div>

                <p class="text-gray-700 dark:text-gray-300 mb-6">
                  Specializing in real-time video streaming, Node.js backend development, and
                  scalable system architecture. With expertise in FFmpeg, WebRTC, and HLS
                  technologies, I build secure and high-performance streaming solutions.
                </p>

                <!-- Social Links -->
                <div class="flex space-x-4">
                  <a
                    href="https://github.com/CodeByAmrit/"
                    target="_blank"
                    class="w-10 h-10 rounded-lg bg-gray-100 dark:bg-gray-700 flex items-center justify-center text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-600 hover:text-blue-600 dark:hover:text-blue-400 transition-colors"
                  >
                    <i class="fab fa-github"></i>
                  </a>
                  <a
                    href="https://www.linkedin.com/in/amrit-sharma-b11b88124/"
                    target="_blank"
                    class="w-10 h-10 rounded-lg bg-gray-100 dark:bg-gray-700 flex items-center justify-center text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-600 hover:text-blue-600 dark:hover:text-blue-400 transition-colors"
                  >
                    <i class="fab fa-linkedin-in"></i>
                  </a>
                  <a
                    href="https://wa.me/919817044885"
                    target="_blank"
                    class="w-10 h-10 rounded-lg bg-gray-100 dark:bg-gray-700 flex items-center justify-center text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-600 hover:text-green-600 dark:hover:text-green-400 transition-colors"
                  >
                    <i class="fab fa-whatsapp"></i>
                  </a>
                </div>
              </div>
            </div>

            <!-- Project Info -->
            <div>
              <div
                class="bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-900/20 dark:to-blue-800/10 rounded-2xl p-8 border border-blue-200 dark:border-blue-800"
              >
                <div class="flex items-center mb-6">
                  <div
                    class="w-14 h-14 rounded-xl bg-gradient-to-br from-blue-500 to-blue-600 flex items-center justify-center mr-4"
                  >
                    <i class="fas fa-users text-white text-2xl"></i>
                  </div>
                  <div>
                    <h3 class="text-2xl font-bold text-gray-900 dark:text-white">Netwon Team</h3>
                    <p class="text-gray-600 dark:text-gray-400">Project Collaborators</p>
                  </div>
                </div>

                <p class="text-gray-700 dark:text-gray-300 mb-6">
                  StreamVision was developed in collaboration with Netwon Team, focusing on creating
                  innovative solutions for real-time surveillance and video streaming applications.
                </p>

                <div class="space-y-4">
                  <div class="flex items-center">
                    <div
                      class="w-10 h-10 rounded-lg bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center mr-4"
                    >
                      <i class="fas fa-check text-blue-600 dark:text-blue-400"></i>
                    </div>
                    <div>
                      <h4 class="font-medium text-gray-900 dark:text-white">
                        Real-time Processing
                      </h4>
                      <p class="text-sm text-gray-600 dark:text-gray-400">
                        Low-latency video streaming
                      </p>
                    </div>
                  </div>

                  <div class="flex items-center">
                    <div
                      class="w-10 h-10 rounded-lg bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center mr-4"
                    >
                      <i class="fas fa-check text-blue-600 dark:text-blue-400"></i>
                    </div>
                    <div>
                      <h4 class="font-medium text-gray-900 dark:text-white">Secure Architecture</h4>
                      <p class="text-sm text-gray-600 dark:text-gray-400">
                        Encrypted end-to-end streams
                      </p>
                    </div>
                  </div>

                  <div class="flex items-center">
                    <div
                      class="w-10 h-10 rounded-lg bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center mr-4"
                    >
                      <i class="fas fa-check text-blue-600 dark:text-blue-400"></i>
                    </div>
                    <div>
                      <h4 class="font-medium text-gray-900 dark:text-white">
                        Scalable Infrastructure
                      </h4>
                      <p class="text-sm text-gray-600 dark:text-gray-400">
                        Handles multiple concurrent streams
                      </p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-50 dark:bg-gray-900 border-t border-blue-100 dark:border-gray-800">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="flex flex-col md:flex-row justify-between items-center">
          <div class="mb-6 md:mb-0">
            <div class="flex items-center mb-4">
              <div
                class="w-10 h-10 rounded-xl bg-gradient-to-br from-[#63b3ed] to-[#3182ce] flex items-center justify-center mr-3"
              >
                <i class="fas fa-video text-white"></i>
              </div>
              <div>
                <div class="text-xl font-bold gradient-text">StreamVision</div>
                <p class="text-sm text-gray-600 dark:text-gray-400">by Netwon Team</p>
              </div>
            </div>
            <p class="text-sm text-gray-600 dark:text-gray-400">
              © 2025 StreamVision. All rights reserved.
            </p>
          </div>

          <div class="flex space-x-6">
            <a
              href="/privacy-policy.html"
              class="text-sm text-gray-600 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 transition-colors"
            >
              Privacy Policy
            </a>
            <a
              href="/terms.html"
              class="text-sm text-gray-600 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 transition-colors"
            >
              Terms of Service
            </a>
            <a
              href="mailto:support@cctvcameralive.in"
              class="text-sm text-gray-600 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 transition-colors"
            >
              Contact Support
            </a>
          </div>
        </div>
      </div>
    </footer>

    <!-- Integrated JavaScript -->
    <script nonce="<%= nonce %>">
      // Theme Toggle
      const themeToggle = document.getElementById("theme-toggle");
      const htmlElement = document.documentElement;

      themeToggle.addEventListener("click", () => {
        htmlElement.classList.toggle("dark");
        localStorage.setItem("theme", htmlElement.classList.contains("dark") ? "dark" : "light");
      });

      // Set initial theme
      const savedTheme = localStorage.getItem("theme") || "light";
      if (savedTheme === "dark") {
        htmlElement.classList.add("dark");
      } else {
        htmlElement.classList.remove("dark");
      }

      // --- Stream Control Logic ---
      const rtspInput = document.getElementById("rtspInput");
      const playBtn = document.getElementById("playBtn");
      const stopBtn = document.getElementById("stopBtn");
      const videoContainer = document.getElementById("videoContainer");
      const videoPlaceholder = document.getElementById("videoPlaceholder");
      const loader = document.getElementById("loader");
      const video = document.getElementById("videoPlayer");
      let currentRtspUrl = "";
      let hls = null;

      // Autoplay from URL Parameter
      window.addEventListener("DOMContentLoaded", () => {
        const params = new URLSearchParams(window.location.search);
        const rtsp = params.get("rtsp");
        if (rtsp) {
          rtspInput.value = rtsp;
          setTimeout(() => playBtn.click(), 1000);
        }
      });

      // Main Play Logic
      playBtn.addEventListener("click", async function () {
        const rtspUrl = rtspInput.value.trim();

        if (!rtspUrl.startsWith("rtsp://")) {
          alert("Please enter a valid RTSP link starting with rtsp://");
          return;
        }

        // Show video container and hide placeholder
        videoContainer.classList.remove("hidden");
        videoPlaceholder.classList.add("hidden");
        loader.classList.remove("hidden");
        video.classList.add("hidden");
        playBtn.disabled = true;

        try {
          const response = await fetch("/api/start-stream", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ rtspUrl }),
          });

          if (!response.ok) {
            const errorData = await response.json();
            throw new Error(errorData.error || "Failed to start stream");
          }

          const data = await response.json();
          const hlsUrl = data.hlsUrl;
          if (!hlsUrl) throw new Error("No HLS URL returned from server");

          currentRtspUrl = rtspUrl;
          loadHlsStream(hlsUrl);
        } catch (err) {
          console.error("Error starting stream:", err);
          alert(`Failed to start stream: ${err.message}`);
          resetPlayerUI();
        }
      });

      // HLS Stream Loading
      function loadHlsStream(hlsUrl) {
        const maxAttempts = 8;
        let attempts = 0;

        const tryLoad = async () => {
          try {
            // Check if the manifest file is available
            const probe = await fetch(hlsUrl, { method: "HEAD" });
            if (probe.ok) {
              loader.classList.add("hidden");
              video.classList.remove("hidden");
              stopBtn.classList.remove("hidden");
              playBtn.disabled = false;

              if (Hls.isSupported()) {
                hls = new Hls();
                hls.loadSource(hlsUrl);
                hls.attachMedia(video);
                hls.on(Hls.Events.MANIFEST_PARSED, () => video.play());
              } else if (video.canPlayType("application/vnd.apple.mpegurl")) {
                video.src = hlsUrl;
                video.addEventListener("loadedmetadata", () => video.play());
              } else {
                alert("HLS is not supported in this browser.");
                resetPlayerUI();
              }
              return;
            }
          } catch (e) {
            // Expected while waiting for file
          }

          if (++attempts < maxAttempts) {
            setTimeout(tryLoad, 2000);
          } else {
            alert(
              "Stream not ready after multiple attempts. Please check the RTSP URL or try again later."
            );
            resetPlayerUI();
          }
        };

        tryLoad();
      }

      // Stop Button Logic
      stopBtn.addEventListener("click", async function () {
        if (!currentRtspUrl) return;

        try {
          const response = await fetch("/api/stop-stream", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ rtspUrl: currentRtspUrl }),
          });
          if (response.ok) {
            resetPlayerUI();
          } else {
            const result = await response.json();
            alert(result.error || "Failed to stop the stream.");
          }
        } catch (err) {
          console.error("Error stopping stream:", err);
          alert("An error occurred while trying to stop the stream.");
        }
      });

      // Reset UI Function
      function resetPlayerUI() {
        if (hls) {
          hls.destroy();
          hls = null;
        }
        video.pause();
        video.src = "";
        video.classList.add("hidden");
        stopBtn.classList.add("hidden");
        loader.classList.add("hidden");
        playBtn.disabled = false;
        currentRtspUrl = "";
        videoContainer.classList.add("hidden");
        videoPlaceholder.classList.remove("hidden");
      }

      // Initialize GSAP animations
      if (typeof gsap !== "undefined" && typeof ScrollTrigger !== "undefined") {
        gsap.registerPlugin(ScrollTrigger);

        // Animate elements with slide-up effect
        gsap.utils.toArray(".card-hover").forEach((elem, i) => {
          gsap.from(elem, {
            scrollTrigger: {
              trigger: elem,
              start: "top 85%",
              toggleActions: "play none none none",
            },
            opacity: 0,
            y: 50,
            duration: 0.8,
            delay: i * 0.1,
            ease: "power3.out",
          });
        });

        // Animate headings
        gsap.utils.toArray("h1, h2, h3").forEach((elem) => {
          gsap.from(elem, {
            scrollTrigger: {
              trigger: elem,
              start: "top 90%",
              toggleActions: "play none none none",
            },
            opacity: 0,
            y: 30,
            duration: 1,
            ease: "power3.out",
          });
        });
      }
    </script>
  </body>
</html>
