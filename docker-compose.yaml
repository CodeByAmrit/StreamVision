version: "3.9"

services:
  streamvision:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streamvision_app
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
